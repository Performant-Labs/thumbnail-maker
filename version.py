"""Single source of truth for the application version.

Imported by the GUI (to display) and by the PyInstaller spec (to embed in the
Windows .exe resource and the macOS .app Info.plist).
"""

__version__ = "1.3.0"
